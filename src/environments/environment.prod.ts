export const environment = {
  production: true,
  // Set to your deployed Socket.io server's public URL (e.g. Railway) before shipping.
  socketUrl: 'https://REPLACE-WITH-RAILWAY-URL.up.railway.app',
};
